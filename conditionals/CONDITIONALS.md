# Conditionals

## if-else statement
- These are used to check condition where if will execute if the condition is true and else will execute if the condition is false

## if-elif-else ladder
- The `elif` here is used to add more conditions for checking.

## pass statement
- This statement is used as a placeholder when we can't decide but need the code block.